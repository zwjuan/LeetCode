* 实现一个栈，要求实现Push（出栈）、Pop（入栈）、Min（返回最小值的操作）的时间复杂度为O（1）。
            方法1：记录最小值，每次入栈时，将最小值也入栈 。 缺点：如果是有序集合，每次将最下值入栈，浪费空间。
            方法2：定义两个栈，正常栈和最小值栈。
* 使用两个栈实现一个队列 （简单）
* 使用两个队列实现一个栈 （简单）
* 元素出栈、入栈顺序的合法性。如入栈的序列（1，2,3,4,5），出栈序列为（4,5,3,2,1）
            不断比较（双循环）
* 一个数组实现两个栈。
* 循环队列的实现